GROQ_API_KEY=your_groq_api_key_here
GROQ_TEXT_MODEL=llama-3.1-8b-instant
# Vision order: Gemini (1st) -> Groq llama-4-scout (2nd) -> Puter.js in browser (3rd)
GROQ_VISION_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# YouTube Data API v3 — embeddable demo videos on exercise/recipe cards
YT_KEY=your_youtube_data_api_key_here

# Local dev only — skip Supabase JWT (pair with VITE_DEV_BYPASS_AUTH=true)
DEV_BYPASS_AUTH=false

# Cloud database — use Supabase SESSION POOLER (port 6543), NOT direct (5432)
# Supabase Dashboard -> Project Settings -> Database -> Connection string -> Session pooler -> URI
DATABASE_URL=postgresql://postgres.your-ref:password@aws-0-ap-south-1.pooler.supabase.com:6543/postgres

# Supabase project URL (Settings -> API -> Project URL)
SUPABASE_URL=https://your-ref.supabase.co

# Supabase JWT secret (Settings -> API -> JWT Secret) — used for legacy HS256 tokens
SUPABASE_JWT_SECRET=your_supabase_jwt_secret